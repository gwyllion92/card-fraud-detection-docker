[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "src"
version = "0.1.0"
description = "Fraud Detection Source Package"
authors = [{ name = "gwyllion92" }]
dependencies = []

[tool.setuptools.packages.find]
where = ["."]